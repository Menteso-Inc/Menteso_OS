{
  "name": "menteso-patentzoom-platform",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/seo-worker",
    "agents/patentzoom_seo_agent"
  ],
  "scripts": {
    "types:build": "npm --workspace @patentzoom/seo-types run build",
    "dashboard:dev": "npm --workspace @patentzoom/seo-types run build && npm --workspace @patentzoom/dashboard run dev",
    "dashboard:build": "npm --workspace @patentzoom/seo-types run build && npm --workspace @patentzoom/dashboard run build",
    "worker:build": "npm --workspace @patentzoom/seo-types run build && npm --workspace patentzoom-seo-agent run build && npm --workspace @patentzoom/seo-worker run build",
    "worker:test": "npm --workspace @patentzoom/seo-worker run test",
    "seo-agent:build": "npm --workspace patentzoom-seo-agent run build"
  }
}
